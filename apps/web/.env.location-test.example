# 少人数ロケテスト用。値を設定したコピーは .env.location-test.local として保存する。
API_BASE_URL=http://127.0.0.1:8000

# 参加者へ伝える共有認証。パスワードは16文字以上にする。
BETA_ACCESS_USER=friend
BETA_ACCESS_PASSWORD=replace-with-a-long-random-password

# FastAPIでPUBLIC_API_TOKENを設定している場合だけ、同じ値を設定する。
API_ACCESS_TOKEN=
