/**
 * 運用者向けの情報を画面へ出してよいかの判定。
 *
 * 取り込みの失敗詳細には、内部のエンドポイントパス・例外文言・再同期の
 * PowerShell コマンドが含まれる。**訪問者はそれを実行できないし、実行すべきでもない。**
 * 手元だけで使っていた頃の画面がそのまま公開されると、構成の内訳を晒したうえに
 * 「自分では直せない指示」を読ませることになる。
 *
 * 既定は非表示。手元やロケテストで運用状況を見たいときだけ環境変数で開ける。
 */
export const showOperatorDetails = process.env.SHOW_OPERATOR_DETAILS?.trim() === "1";
